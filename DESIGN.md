# JIN Studio · 视觉与交互

更新：2026-09-26。

## 舞台

暖白 `#f3ede6` 与墨色 `#282522` 给角色图和影片留空间。桌面左栏 34%、右场 66%，中间细线分隔。圆心在左边界之外，停稳卡在左栏半弧最右点；画布裁切在分隔线之前，卡片和黏液不盖住右场。SDF、细丝、玻璃折射未改。角色竖图在 3:2 图集单元中等比留白。片帧维持原宽高比；Facebook 封面有平台角标，《家乡》保留示范片标记。

渡鸦沿用已有 mark，作为 favicon 与左上角回到第一张卡的按钮。入口标题是 JIN Studio。当前八张可用卡沿环排布，邻卡从上下边缘露出。左栏上下放置原有编号/名称及类型/年份变形文字；右场按微标、标题、媒体、描述与行动按钮排列，右上索引保留。内容约 0.65 秒交叉淡入，快速旋转期间保持上次停稳内容。

## 操作

- 桌面鼠标仅在左栏上方或下方驱动区持续转向，越靠边缘越快；屏幕中线两侧各 12% 是静区。移入静区、右场或离开窗口后滑行并吸卡。滚轮保留为较柔和的辅助输入，拖拽、索引和键盘仍可直接选卡。选卡转场及浮层不会同时受到区域输入。
- 新卡停稳后，卡图在环下方模糊淡入；影片在桌面增加同片静音循环模糊预览，切走立即暂停、淡出。快速经过其他卡时背景保持上一次画面。Contact 显示中性纸色，浅色遮罩保证文字可读。
- 非居中卡和索引：平滑转环；居中卡/右场按钮：放大图、影院浮层或外跳原帖。缺图的服装三视图暂不进环。右场原生影片默认暂停，在 16:9 容器内保持原片比例，三小只竖片不会被裁成横片。
- URL hash 与停稳卡同步，浏览器前进后退反向转环。方向键、Home/End、数字键和可聚焦的索引按钮形成键盘路径。
- 768px 以下使用单列内容舞台及顶部作品选择框，隐藏半弧和区域驱动，背景只用静帧。宽度阈值从 `params.js` 传给根样式类，可在开发面板调整。
- 减少动态效果时跳过入场和元数据黏液转场；播放始终由用户启动。没有 JavaScript/WebGL 时显示图、名称、类型和可用链接的滚动列表。
- 弹窗内焦点移入，Esc/关闭/遮罩退出，关闭后焦点回到按钮；本地影片关闭或离开舞台时暂停并回到开头。右场播放器按 Esc 暂停复位并把焦点交还行动按钮。

## 字体与来源

Satoshi/Geist WOFF2 来自上游允许商用的已附字体；删除上游 PP Neue Montreal 和 18 张示例作品图。保留上游 MIT 许可、shader 和技术说明的署名；用户提供的 IP 图、影片和渡鸦属于 JIN Studio 的站点素材。

## 花园与昼夜

花园采用固定镜位的图像背景与 Three.js 水车合成。双轮圈、八根辐条、十二组接水斗组成 `Rotor`；轴、前后 A 形支架和引水槽固定。木纹以确定性程序纹理生成，金属、干木和湿木分别用 PBR 材质。白天使用偏暖主光；夜间转为月光与右侧暖色点光，背景同步淡入。

花园中心水车为主交互，下方提供明确的文字入口。路灯热区随背景 cover 的实际裁切坐标计算；窄屏向右取景保留路灯，模型独立缩放后放在溪边。昼夜共用几何和机位，模型不随背景切换跳位。

默认转速 2.2 rpm、渲染上限 30 fps、DPR 上限 1.5；入口离开后整个花园释放 WebGL 资源，再挂载作品轮盘，避免两套渲染同时运行。水流为轻量粒子示意。暂停、后台标签和减少动态均停止水车运动。

`--paper` / `--ink` / `--rule` / `--muted` / `--accent` / `--surface-rgb` 在 `html[data-theme]` 下管理昼夜颜色，右场、索引、目录和影片遮罩共享主题；环只更新现有颜色 uniform，不改 shader。
