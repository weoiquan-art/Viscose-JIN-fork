# JIN Studio · 视觉与交互

更新：2026-09-25。

## 舞台

暖白 `#f3ede6` 与墨色 `#282522` 给角色图和影片留空间。圆环的中心藏在屏幕左侧，正面卡在视口中心；相邻卡保持 Viscose 的黏液、细丝、悬停让位与玻璃折射。角色竖图在 3:2 图集单元中等比留白，避免裁掉人物。片帧维持原宽高比；Facebook 封面上有平台角标，《家乡》封面有“示范片”标记。

渡鸦沿用已有 mark，作为 favicon 与左上角回到第一张卡的按钮。入口标题是 JIN Studio。九张卡使用较小半径，让邻卡仍从上下边缘露出；正面卡按实际半径居中。索引位于右上；停稳时沿用左侧编号与名称、右侧类型与年份（未知年份留空）的文字过渡，下方才出现一句说明和行动按钮。

## 操作

- 非居中卡和索引：平滑转环；居中卡/详情按钮：放大图、原生视频或外跳原帖。服装三视图卡明确不能打开不存在的图。
- URL hash 与停稳卡同步，浏览器前进后退反向转环。方向键、Home/End、数字键和可聚焦的索引按钮形成键盘路径。
- 手机使用顶部作品选择框，底部简短说明和操作。正面卡的环中心依据实际半径定位，避免 360–640px 固定偏移。
- 减少动态效果时跳过入场和元数据黏液转场；播放始终由用户启动。没有 JavaScript/WebGL 时显示图、名称、类型和可用链接的滚动列表。
- 弹窗内焦点移入，Esc/关闭/遮罩退出，关闭后焦点回到按钮；本地影片关闭时暂停并回到开头。

## 字体与来源

Satoshi/Geist WOFF2 来自上游允许商用的已附字体；删除上游 PP Neue Montreal 和 18 张示例作品图。保留上游 MIT 许可、shader 和技术说明的署名；用户提供的 IP 图、影片和渡鸦属于 JIN Studio 的站点素材。
