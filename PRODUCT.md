# JIN Studio · 产品定义

更新：2026-09-26。此 Fork 是 Cloudflare Pages 上的预览候选。

## 内容与行为

- 当前可导航顺序：糯糯、菲比、Sera Q 版、Sera 拟真肖像、三小只影片、Sera《家乡》非正史试片、三小只 Facebook 原帖、Contact。服装三视图缺图，保留素材记录但暂不生成空卡。
- 每张卡的 `slug` 同时作为 URL hash；停稳后更新地址，支持刷新、直达和前进后退。
- 两部 MP4 在右侧舞台提供默认暂停的原生播放器；桌面停稳时在文字后方启动静音模糊预览，触控和减少动态效果时改用静帧。《家乡》标为示范试片，明确非正史；Facebook 卡只外跳，不内嵌或镜像社交影片。
- Contact 使用用户提供的邮箱、Instagram、TikTok、课程站，以及用户 GitHub 账号链接。不显示真实姓名、团队规模、客户或虚构成绩。
- 右侧舞台按 `portrait`、`film`、`social`、`contact` 渲染当前内容；`dossier`、`sheet`、`process`、`scene` 的版式已准备好，收到真实图文后再建卡。Facebook 帖子专用封面待提供，当前仍用已有本地影片静帧。
- 桌面左栏占视口 34%，半弧卡在左栏中心附近停稳；右侧舞台占 66%，只在停稳后换内容。左栏上下区域驱动轮盘，中线两侧各 12% 为静区，滚轮仍可辅助。

## 技术与发布

- Next.js 16、React 19、Three.js、GSAP、Tailwind v4，基于 Viscose 原 shader 环形轮盘。旧版“保持纯静态 HTML 不迁移框架”决定在此 Fork 作废。
- 渡鸦只作为 favicon 和可点击的角落标记；旧版大型 Hero 与六章节 Canvas 2D 草稿属于先前探索，本次整站为单轮盘。
- 提供键盘、减少动态效果、小屏作品选择框、无 JS/WebGL 的完整目录降级；小屏不显示 WebGL 半弧。
- Cloudflare Pages 使用本 Fork 的静态导出 `out/`。本分支作为预览供用户检查；正式发布和替换现有站点仍由用户在预览后决定。

## 2026-09-26 · 花园入口与水车建模

访客从花园进入作品世界：真实 3D 水车缓慢转动，点击水车或“进入作品世界”进入既有作品轮盘。路灯和右上按钮切换昼夜，共用 localStorage 主题；进入作品站后主题保留。水车可暂停，系统减少动态设置下保持静止。作品深链接直接打开对应卡片；从花园进站后的浏览器后退可返回花园。

用户明确选择局部建模：水车使用独立可旋转网格，花园使用用户概念图编辑得到的底图。当前为可审核第一版模型，不是全场景 3D，也不是流体模拟。正式发布仍待预览审阅。
