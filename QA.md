# QA 记录

## 2026-09-26 · 左半弧 / 右场预览候选

| 检查 | 状态 | 证据/范围 |
| --- | --- | --- |
| `npm run build`、`npm run lint`、`git diff --check` | 已通过 | 静态导出到 `out/`，无 lint 错误；未改 shader |
| 半弧、舞台与无横向溢出 | 已通过 | Chromium 软件 WebGL 截图及 DOM 尺寸：1440×900 左栏 490px/右场 950px，800×700 左栏 272px/右场 528px；窄屏旧偏移修正后卡面和相邻弧可见，裁切不侵入舞台。640×800、360×780 单列，无文档横向溢出 |
| 左栏区域、静区、右场互斥、拖拽与吸卡 | 已通过 | 800×600、DPR 0.25：左栏中心与右场均 idle；上区停稳由 `#nuonuo` 到 `#contact`，下区返回 `#nuonuo`；驱动中 hash 不逐卡变化。左栏拖拽从 `#nuonuo` 滑行吸附到 `#phoebe`，无运行时错误 |
| 舞台内容与影片 | 已通过 | 800×700：《家乡》右场 444×250 的 16:9 原生播放器默认暂停；静音自动背景预览使用同一 MP4；社媒卡换场后播放器卸载、背景片暂停淡出，原帖链接为新标签且含 `noopener noreferrer` |
| 键盘、历史、焦点 | 已通过 | 图片浮层 Esc 后焦点回行动按钮；随后 End/ Home/数字 6 及浏览器后退均切换正确卡和 hash。右场视频 Esc 暂停复位、焦点回行动按钮；修正了直达锚点后字体备用计时器重复初始化的问题 |
| 移动/减少动态效果 | 已通过 | 360px 与 640px 隐藏 WebGL 半弧并跳过画布绘制，顶部选择框可直达影片，背景视频未挂载；reduced-motion 下区域提示和背景视频均关闭，Home 仍可用 |
| 无 JS / WebGL 失败 | 已通过 | 两种情况均显示八张可滚动的真实内容卡与可用链接；缺图三视图不生成空卡 |
| 浏览器运行时 | 已通过 | 软件 WebGL 加载各布局，无 page error；GLSL 未修改 |
| Cloudflare 分支预览 | 已部署并核对降级 | 代码提交 `89deb84` 的 [Cloudflare 预览](https://7be84fbd.viscose-jin-fork.pages.dev/) 构建成功；云浏览器禁用 WebGL 时在线显示八张真实内容目录 |
| 实体设备 | 待用户审阅 | 鼠标转向手感、手机实机与媒体带宽仍需在用户设备上核对 |

旧记录为前一版整屏轮盘的历史 QA；其中九张卡和全视口区域驱动已由上表的八张可用卡、左栏区域取代。

## 2026-09-26 · 互动优化

| 检查 | 状态 | 证据/待做 |
| --- | --- | --- |
| `npm run lint`、`npm run build`、`git diff --check` | 已通过 | 本分支本地运行；静态导出到 `out/` |
| 上/下区域、对称静区与停稳吸卡 | 已通过 | Chromium 软件 WebGL，800×600、DPR 0.5：从 Sera 肖像上转至糯糯、再下转至后续卡；持续驱动时 hash 未跳，停稳后更新 |
| 辅助滚轮与键盘 | 已通过 | 800×600 连续滚轮从糯糯转至菲比；Home 返回糯糯；无页面错误 |
| 背景交叉淡入、无图回退 | 已通过 | 新卡停稳后画面层切为糯糯图，旧肖像层淡出；Contact 两层透明，返回纸色 |
| 图片浮层、Esc/焦点、历史导航 | 已通过 | 1440px 实测肖像浮层、Esc 关闭焦点返回、浏览器后退回原卡；无页面错误 |
| 减少动态效果 | 已通过 | 640px 环境：鼠标区提示隐藏且不驱动；方向键仍切卡 |
| 手机触控与作品下拉 | 已通过 | 360px 触控模拟：纵向滑动从糯糯切至菲比；下拉直达三小只影片 |
| 无 JS / WebGL 与背景目录 | 已通过 | 禁 JS 显示九张可滚动目录与卡图背景；禁 WebGL 显示九张目录 |
| 1440 / 640 / 360px 排版 | 已通过 | Chromium 截图检查，无文档横向溢出；文本、人物卡和背景可见 |
| Cloudflare 新预览与实体设备手感 | 待核对 | PR 分支提交后检查新预览 URL；鼠标区域手感与手机实机仍由用户审阅 |

更新：2026-09-25。每项只在实际完成后标记通过；源码编译并不等于浏览器中的 GLSL 成功编译。

| 检查 | 状态 | 证据/待做 |
| --- | --- | --- |
| `npm ci` | 已通过 | 上游源码基线安装 |
| `npm run lint` | 已通过 | 本分支执行；无报错及警告 |
| `npm run build` | 已通过 | Next.js 16 静态页面编译完成 |
| 360 / 640 / 1024 / 1512px 浏览器排版 | 已通过 | Chromium 153 截图，Sera 肖像和资讯显示，无文档横向溢出 |
| WebGL shader 运行与无控制台报错 | 已通过 | 实际 Chromium 153 加载 WebGL 场景，页面及控制台无报错；GLSL 未改 |
| hash 直达、刷新、前进后退 | 已通过 | 直达肖像，索引切卡，浏览器前进后退回到正确 hash 与资讯 |
| 普通模式入场、索引、滚轮、键盘与触控 | 已通过 | 无 hash 的入场到糯糯、滚轮切至菲比、CDP 手机触控切卡；索引、Home/End、数字键已通过 |
| reduced-motion 与无 JS/WebGL 降级 | 已通过 | 减少动态效果直接到指定卡；禁 JS 显示九张目录；强制 WebGL 失败也显示目录 |
| 两部影片播放、关闭、复位及焦点 | 已通过 | 两部均打开正确本地 MP4；Esc/关闭按钮可退出，焦点回到行动按钮；视频元素卸载且清理时暂停复位 |
| Facebook 新标签与 `noopener noreferrer` | 已通过 | Chromium DOM 核对原帖 URL、`target="_blank"`、`rel="noopener noreferrer"` |
| Cloudflare 预览地址与正式发布 | 旧提交预览已部署 | PR #1 的旧提交 `dd7ef10` 已有 Pages 预览；互动优化的预览待新提交后核对。正式发布由用户决定 |

上游原始 `npm run build` 与 `npm run lint` 也已通过。本次未改 `planeShaders.js` 的 GLSL 内容。检查使用本地无头 Chromium 的软件 WebGL；1024px 滚轮检查以设备像素比 0.5 减轻软件渲染负担。手机触控仍需在实体设备上最终视觉审阅。
