# 变更记录

## 2026-09-26 · 左半弧与内容舞台（预览分支）

- WebGL 圆心移到视口左外，停稳卡位于占屏 34% 的左栏；画布裁在分隔线之前。环的 shader、黏液、细丝和玻璃逻辑保持原样。原有双侧 meta 改放在左栏上下，索引仍在右上。
- 右场新增随停稳卡交叉淡入的标题、主内容、简介和操作；两部本地影片在 16:9 原生播放器中按需播放，桌面背景复用原片做静音低透明度预览，离开后暂停淡出。社媒卡继续只打开原帖。
- 区域驱动限定左栏；移入右场或静区即归零，仍由原阻尼和吸卡结尾。移动端改为单列舞台加作品选择框，隐藏半弧与背景动片。减少动态效果也关闭动片。
- `catalog.js` 从原 `projects.js` 派生当前可用卡和布局类型，缺图的三视图暂不进入环；新档案、设定、过程与场景版式可接真实素材，不创建假卡。当前八张卡及素材记录均可追溯。
- 新默认参数：`navWidth=0.34`、`navFront=0.17`、`navCardFit=0.9`、`mobileAt=768`、`stagePadding=42`、`stageTime=0.65`、`navMetaTop=14`、`navMetaBottom=13`、`bgVideoOpacity=0.22`，可在开发面板调整。代码提交 `89deb84` 的 [Cloudflare 分支预览](https://7be84fbd.viscose-jin-fork.pages.dev/) 已部署；正式发布待用户审阅。

## 2026-09-26 · 轮盘互动优化（预览分支）

- 鼠标在视口中线以上或以下持续驱动轮盘；中线两侧各 12% 高度是对称静区。速度按距离平滑变化，离开驱动区后沿用原阻尼与吸卡；指针压在正面卡、拖拽、选卡转场、影片/图片浮层和减少动态效果时暂停区域驱动。触控继续使用原有滑动与作品下拉。
- 滚轮改为辅助的目标速度输入；默认 `scrollSpeed=0.0018`、`wheelDecay=0.94`、`damping=0.965`、`snapTime=1.05`、`snapFrom=0.7`。区域参数 `zoneDead=0.12`、`zoneMaxSpeed=1.4`、`zoneChase=0.16`、`zoneCurve=1.3`、`zoneHint=0.16`、`zoneInvert=false`，均可在开发面板调整。
- 卡片停稳后才让相关图片或影片封面在 canvas 下交叉淡入。背景可由作品 `bg` 覆盖，缺省复用卡图/海报；Contact 与待补素材回到中性纸色。默认 `bgOpacity=0.26`、`bgBlur=38px`、`bgTime=1s`；小屏降低模糊半径，静态目录也使用对应图片。
- 未改 SDF、黏液与细丝 shader，也未新增运行时依赖。正式版需在 Cloudflare 预览与用户审阅后再发布。

## 2026-09-25 · JIN Studio 轮盘预览候选

- 从 Viscose 源码改造单轮盘站，保留 WebGL SDF shader、图集、GSAP 和调参结构，未重写核心交互。
- 以四张用户提供角色图、两部 MP4 的真实帧、明确标注的三视图占位、一个 Facebook 原帖和 Contact 取代上游示例作品。
- 新增锚点深链接、历史导航、可点击索引、键盘操作、类型对应详情与原生影片浮层。
- 修正手机正面卡定位，支持减少动态效果；无 JS/WebGL 时显示完整作品目录。
- 修复软件 WebGL 低帧率下入场时间轴被滞后平滑拖慢的问题，普通模式可完成入场并响应滚轮和触控。
- 渡鸦转为 favicon/角标，取消旧版大型 Hero；改用 Next.js Fork，旧版六章节 Canvas 2D PR 只作历史参考。
- 删除商业字体和 18 张第三方示例卡图；Satoshi/Geist 转 WOFF2。更新产品、设计、素材和 QA 文档。

正式发布：待用户审核预览后进行。

## 2026-09-26 · 可旋转 3D 水车花园入口（预览）

- 新增花园入口和独立 Three.js 水车模型，木制轮圈、辐条、接水斗、轴与固定支架分组；提供可复用 GLB 与透明降级海报。
- 从用户概念图制作无水车的昼夜底图，模型采用同一机位与几何，月光和暖灯随主题切换。路灯及全站按钮同步主题并持久化。
- 点击水车/入口按钮播放涟漪淡出后进入既有半弧作品站，保留作品 hash、后退/前进；深链接跳过入口；站内可返回花园。
- 手机独立安排模型尺寸与背景裁切，保留灯的点击位置；增加暂停动态、减少动态、后台暂停、WebGL 失败时模型海报和无 JS 静态目录。
- 未增加运行时依赖，未改原环的 SDF、黏液或细丝 shader。模型与素材细节见 `public/garden/README.md`。
